# .github/workflows/main.yml

name: lsl-tools

# Controls when the workflow will run
on:
  push:
  pull_request:
    branches: [ "main" ]

jobs:

  build-latest:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v4

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install wxPython dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev libtiff-dev libjpeg-dev libnotify-dev libsdl2-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev

      - name: install
        run: |
          python -m pip install --upgrade pip
          pip install .[dev]
      - name: lint
        run: black --check .

      - name: type-check
        run: mypy ./

      - name: test
        env:
          LD_LIBRARY_PATH: /usr/share/miniconda/lib:$LD_LIBRARY_PATH
        shell: bash
        run: |
          conda install -c conda-forge liblsl
          coverage run --branch -m unittest discover
          coverage report -m
          coverage xml
